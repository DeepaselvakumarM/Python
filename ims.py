class Products:
    def __init__(self)
        self.products={}
        def add(self,name,quantity,price):
            self.Products["name"]=name,
            self.Products["quantity"]=quantity,
            self.Products["price"]=price,
        def remove(self,data):
            del self.products[data]
def main(option):
    if option==1:
        Products=Products()
        Products.add()
    if option==2:
        Products=Products()
        Products.remove()

    
print("Welcome to IMS")
print("click 1 for add item")
print("click 2 for remove item")
choice=input("enter your option")
if choice==1:
    main(choice)
elif choice==2:
    main(choice)

# walkthrough